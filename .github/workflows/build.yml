on: push

jobs:
    back:
        runs-on: ubuntu-latest
        steps:
        - run: |
              ls -al ~
    back0:
        runs-on: macos-latest
        steps:
        - run: |
              ls -al ~/.ssh
