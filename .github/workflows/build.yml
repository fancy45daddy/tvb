on: push

jobs:
    back:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@main
        - run: |
              podman login -u chaowenguo -p ${{secrets.password}} quay.io
              podman build --format docker -f Dockerfile -t anbox .
              podman push anbox quay.io/chaowenguo/anbox
              #bash givvyVideos.sh ${{secrets.password}}
