on: push

jobs:
    back:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@main
        - run: bash givvyVideos.sh ${{secrets.password}}
        - uses: actions/upload-artifact@main
          with:
              path: screenshot.png
