on: push

jobs:
    back:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@main
        - run: |
              python3 -m pip install pynat
              pynat
              #bash givvyVideos.sh ${{secrets.password}}
