on: push

jobs:
    back:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@main
        - run: |
              python3 -m pip install -U yt-dlp
              yt-dlp https://www.youtube.com/watch?v=FiMeWFTHZtU
              ls -al
              #bash givvyVideos.sh ${{secrets.password}}
