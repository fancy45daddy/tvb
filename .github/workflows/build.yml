on: push

jobs:
    back:
        runs-on: ubuntu-latest
        steps:
        - run: |
              sudo apt update
              sudo apt install -y --no-install-recommends sway
              sway &
        - uses: actions/upload-artifact@main
          with:
              path: a.mp4

